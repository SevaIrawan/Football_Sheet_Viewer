"use client";

import { MatchViewer } from "@/components/MatchViewer";

/**
 * Sementara: Schedule pakai UI yang sama dengan Result.
 * Nanti kita optimasi: ganti data source, tab, dan komponen yang perlu.
 */
export function ScheduleViewer() {
  return <MatchViewer />;
}

